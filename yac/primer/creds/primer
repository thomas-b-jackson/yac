
YAC - Your Automated Cloud v{VER}
{COMMAND}

Available comands include:

  yac {COMMAND} (-h | primer)

* -h provides cli help
* primer will display this page

Generate a set of credentials per a credentialer specified in a servicefile, e.g.:

$ yac creds <abs or rel path>/<servicefile> <credentialer name>

Supported credentialers include:

* nordstrom-aws (via ~/.aws/credentials), and
* nordstrom-k8s (via ~/.kube/config)